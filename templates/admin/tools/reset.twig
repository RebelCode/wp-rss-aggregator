<h3>{% trans "Delete all imported items" %}</h3>
<p>{% trans "Click the red button below to delete all imported items (and posts)." %}</p>
<p><em>{% trans "This can take a long time if you have a lot of feed sources." %}</em></p>

<form method="post">
    {{ wp_nonce_field('wpra_delete_all_items', 'wpra_delete_all_items_nonce') }}

    <input type="submit"
           class="button button-red"
           name="wpra_delete_all_items"
           value="{{ "Delete all imported items"|trans|e('html_attr') }}"
    />
</form>

<h3>{% trans 'Restore Default Settings' %}</h3>

<p>{% trans 'Click the red button to reset the plugin settings to default.' %}</p>
<p>{% trans '<em><strong>Note:</strong> This cannot be undone. Once the settings have been reset, your old settings cannot be restored.</em>' %}</p>

<form method="post">
    {{ wp_nonce_field('wpra_reset_settings', 'wpra_reset_settings_nonce') }}

    <input type="submit"
           class="button button-red"
           name="wpra_reset_settings"
           value="{{ "Reset Settings to Default"|trans|e('html_attr') }}"
    />
</form>
